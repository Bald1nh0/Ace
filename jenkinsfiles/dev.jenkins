#!groovy
// Run build
properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        label 'master'
        }
    triggers { pollSCM('* * * * *') }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
   stages {
        stage("First step") {
            steps {
                sh 'ssh root@ec2-35-180-25-23.eu-west-3.compute.amazonaws.com \'hostname\''
            }
        }
        stage("Second step") {
            steps {
                sh 'ssh root@ec2-35-180-25-23.eu-west-3.compute.amazonaws.com \'uptime\''
            }
        }
    }
}
