# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - staging
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_STAGE_C294C }}'
          channelId: live
          projectId: stage-c294c
      - name: dsds 
        run: |
                curl -X POST --data-urlencode "payload={\"channel\": \"#firebase\", \"username\": \"webhookbot\", \"text\": \"This is posted to #firebase and comes from a bot named webhookbot.\", \"icon_emoji\": \":ghost:\"}" \
                https://hooks.slack.com/services/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

